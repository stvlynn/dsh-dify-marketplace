# Segments

Inside a slice, files group by technical role:

| Segment | Contains |
|---|---|
| `ui` | React components |
| `model` | View-model / local state |
| `lib` | Pure helpers |
| `api` | Host HTTP/Remote calls (features only) |
| `config` | Constants |

Not every slice needs every segment.
